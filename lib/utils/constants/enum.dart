enum TextSizes { small, medium, large }

enum OrderStatus { pending, proccessing, shipped, delivered, cancelled }

enum ProductType { single, variable }

enum PaymentMethod {
  paypal,
  googlepay,
  applePay,
  visa,
  mastercard,
  cerditCard,
  payStack,
  razorPay,
  paytm
}
